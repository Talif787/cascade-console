# Base URL of the running Cascade FastAPI control plane.
CASCADE_API_URL=http://localhost:8000

# Optional bearer token attached server-side by the BFF proxy. Leave unset when
# running the backend locally with CASCADE_AUTH_ENABLED=false.
# CASCADE_API_TOKEN=
